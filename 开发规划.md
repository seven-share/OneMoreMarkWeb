# OneMoreMark VitePress 主题开发规划

## 主题定位

OneMoreMark Web 是一个内容型 VitePress 自定义主题，目标不是传统文档站，而是面向工具、资源和知识管理的双语内容网站。

主题应持续保持三个核心特征：

- 内容驱动：文章和分类都从 Markdown frontmatter 读取。
- 静态部署：不依赖后端、数据库或动态接口。
- 可维护：新增分类、文章、图标和语言时尽量只改内容文件。

## 当前实现状态

已完成：

- 首页、分类页、文章页三类核心页面。
- 中文 `/zh/` 和英文 `/en/` 双语结构。
- 根路径 `/` 跳转到可配置默认语言。
- 文章数据加载器 `posts.data.ts`。
- 分类数据加载器 `categories.data.ts`。
- 分类 SVG 图标目录 `public/icons/category/`。
- 热门文章通过 `hot: true` 标识。
- 分类页文章置顶与日期排序。
- 分类页分页，当前每页 10 篇。
- 顶部语言切换。
- 顶部明暗主题切换。
- Cloudflare Pages 静态部署能力。

## 信息架构

### 路由

```txt
/                         根路径，跳转到默认语言
/zh/                      中文首页
/zh/category/:slug        中文分类页
/zh/posts/:slug           中文文章页
/en/                      英文首页
/en/category/:slug        英文分类页
/en/posts/:slug           英文文章页
```

默认语言在 `.vitepress/config.ts` 中配置：

```ts
themeConfig: {
  defaultLocale: 'zh'
}
```

### 页面类型

页面由 frontmatter 的 `layout` 决定：

| layout | 组件 | 用途 |
| --- | --- | --- |
| `home` | `HomePage.vue` | 首页 |
| `category` | `CategoryPage.vue` | 分类详情页 |
| `post` | `PostPage.vue` | 文章详情页 |

`Layout.vue` 是主题布局分发入口。

## 内容模型

### 文章

文章目录：

```txt
zh/posts/
en/posts/
```

文章 frontmatter：

```md
---
layout: post
title: AI 工具的未来趋势
category: AI 工具
categorySlug: ai-tools
date: 2024-05-25
cover: /images/articles/category.png
hot: true
pinned: true
---
```

字段说明：

| 字段 | 作用 |
| --- | --- |
| `layout` | 文章页使用 `post` |
| `title` | 文章标题 |
| `category` | 当前语言下展示的分类名 |
| `categorySlug` | 分类标识，用于筛选 |
| `date` | 发布时间和排序依据 |
| `cover` | 封面图 |
| `hot` | 是否进入热门文章模块 |
| `pinned` | 是否在分类页置顶 |

排序规则：

1. `pinned: true` 排在普通文章前面。
2. 同一置顶状态下，按 `date` 倒序排列。

### 分类

分类目录：

```txt
zh/category/
en/category/
```

分类 frontmatter：

```md
---
layout: category
title: AI 工具
category: AI 工具
categorySlug: ai-tools
icon: ai-tools
order: 3
---
```

字段说明：

| 字段 | 作用 |
| --- | --- |
| `layout` | 分类页使用 `category` |
| `title` | 页面标题 |
| `category` | 当前语言下展示的分类名 |
| `categorySlug` | 分类标识 |
| `icon` | SVG 图标文件名 |
| `order` | 分类排序权重 |

分类排序规则：

1. `order` 数字小的排前面。
2. `order` 相同则随机排序。

分类不再承担“热门文章”含义。热门文章只通过文章的 `hot: true` 标识。

## 数据加载

### 文章加载器

文件：

```txt
.vitepress/theme/data/posts.data.ts
```

职责：

- 读取 `zh/posts/*.md` 和 `en/posts/*.md`。
- 从路径判断语言。
- 输出文章列表。
- 处理 `hot`、`pinned` 等布尔字段。
- 按置顶和日期排序。

### 分类加载器

文件：

```txt
.vitepress/theme/data/categories.data.ts
```

职责：

- 读取 `zh/category/*.md` 和 `en/category/*.md`。
- 按语言分别输出分类，不合并中英文分类。
- 读取 `icon` 和 `order`。
- 按 `order` 排序，相同 `order` 随机排序。

## 组件规划

### 页面组件

`HomePage.vue`

- 首页布局。
- 今日推荐读取 `categorySlug: recommend`。
- 精选文章读取 `categorySlug: featured`。
- 分类浏览读取当前语言分类，排除 `recommend` 和 `featured`。

`CategoryPage.vue`

- 分类详情页。
- 根据当前分类筛选文章。
- 支持分页。
- 右侧展示分类目录和热门文章。

`PostPage.vue`

- 文章详情页。
- 展示文章标题、日期、分类和正文。
- 右侧展示分类目录和相关文章。

### 通用组件

`SiteHeader.vue`

- Logo。
- 关于入口。
- 语言切换。
- 明暗主题切换。

`LanguageSelect.vue`

- 在 `/zh/` 和 `/en/` 路径之间切换。
- 当前语言页面不存在时，后续可增强为回退到语言首页。

`ThemeToggle.vue`

- 切换 `html.dark`。
- 将选择保存到 `localStorage.om-theme`。
- 首次访问跟随系统偏好。

`CategoryCard.vue`

- 首页分类卡片。
- 读取本地 SVG 分类图标。

`SidebarCategories.vue`

- 显示当前语言分类目录。

`SidebarPosts.vue`

- 分类页用于热门文章。
- 文章页用于相关文章。

## 样式规划

样式集中在：

```txt
.vitepress/theme/style.css
```

当前样式策略：

- 使用 CSS 变量维护颜色。
- 深色模式通过 `html.dark` 覆盖变量。
- 卡片圆角保持 8px。
- 内容区和侧边栏保持响应式单列降级。
- 分类图标使用固定尺寸 SVG，避免字符图标风格不统一。

后续优化方向：

- 抽离更清晰的主题变量段。
- 给按钮、卡片、面板建立更稳定的状态样式。
- 优化移动端顶部导航的空间占用。
- 减少页面中重复的硬编码颜色。

## 图标规划

分类图标目录：

```txt
public/icons/category/
```

规则：

- 文件名与分类 frontmatter 的 `icon` 一致。
- 使用 SVG。
- 不依赖外部 iconfont 脚本。
- 可从阿里矢量图标下载 SVG 后放入目录。

示例：

```txt
public/icons/category/ai-tools.svg
```

```md
icon: ai-tools
```

## 多语言规划

当前语言：

- 中文：`zh`
- English：`en`

约定：

- 中英文文章建议使用相同 slug。
- 中英文分类建议使用相同 `categorySlug`。
- 展示文本由当前语言 Markdown frontmatter 决定。
- 分类数据不合并中英文，各语言独立读取。

后续新增语言时，需要：

1. 在 `.vitepress/config.ts` 中增加 locale。
2. 增加对应语言目录，例如 `ja/`。
3. 扩展 `Locale` 类型。
4. 扩展 `LanguageSelect.vue`。
5. 调整内容加载器路径。

## 功能迭代路线

### 短期

- 增加空状态：分类无文章、热门文章为空、相关文章为空。
- 优化语言切换：目标页面不存在时自动跳转到对应语言首页。
- 优化深色模式首次加载闪烁问题。
- 将分页 page size 抽到主题配置。
- 为分类排序随机逻辑增加稳定随机种子，避免每次构建顺序不可控。

### 中期

- 增加站内搜索。
- 增加标签体系。
- 增加文章归档页。
- 增加 SEO 元信息和 Open Graph 图片。
- 增加 sitemap 和 robots 配置。
- 增加 404 页面。

### 长期

- 支持更多语言。
- 支持内容集合校验脚本，检查分类 slug、图标文件、双语文章缺失。
- 支持主题配置化，例如首页模块数量、分页数量、默认语言、导航项。
- 支持更完整的可访问性检查。

## 当前不做

- 登录注册。
- 评论系统。
- 后台管理。
- 数据库。
- 动态接口。
- 付费内容。
- 复杂 CMS 集成。

## 验收标准

- `/` 能跳转到默认语言首页。
- `/zh/` 和 `/en/` 首页正常显示。
- 分类页能按当前语言和分类筛选文章。
- 文章页能正常渲染 Markdown 正文。
- 热门文章来自 `hot: true`。
- 分类图标来自本地 SVG。
- 明暗主题切换可用，并能记住选择。
- 分类页超过 10 篇文章时显示分页。
- 执行 `npm run build` 可以生成静态文件。
- 生成结果可部署到 Cloudflare Pages。
